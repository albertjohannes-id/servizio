{
  "name": "servizio-api",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute servizio --local --file=./schema.sql",
    "db:remote": "wrangler d1 execute servizio --remote --file=./schema.sql",
    "types": "wrangler types",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20260702.1",
    "typescript": "^5.9.2",
    "wrangler": "4.34.0"
  }
}
